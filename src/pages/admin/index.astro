---
// COMPLETELY OPEN ACCESS (TEMPORARY)
import { getCollection } from "astro:content";
const posts = await getCollection("post");

console.log("Posts found:", posts.length); // Check server logs
---
<html>
<body>
  <h1>DEBUG MODE: Admin Panel</h1>
  <p>If you see this, the page loaded successfully.</p>
  <pre>{JSON.stringify(posts, null, 2)}</pre>
</body>
</html>