---
// src/pages/[slug].astro
import Layout from '../../layouts/Layout.astro';

// Define your static pages here (outside getStaticPaths)
const staticPages = [
  { slug: 'home', title: 'Home', description: 'Welcome to my site' },
  { slug: 'about', title: 'About', description: 'About me' },
  { slug: 'contact', title: 'Contact', description: 'Get in touch' }
];

export async function getStaticPaths() {
  return staticPages.map((page) => ({
    params: { slug: page.slug },
    props: { page }
  }));
}

const { page } = Astro.props;
---

<Layout 
  title={`${page.title} - The Mwaniki's Report`}
  description={page.description}
>
  <article class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4 leading-tight">
      {page.title}
    </h1>
    
    <div class="prose prose-lg max-w-none">
      {page.slug === 'home' && (
        <p>Welcome to my homepage!</p>
      )}
      {page.slug === 'about' && (
        <p>About me content goes here...</p>
      )}
      {page.slug === 'contact' && (
        <p>Contact form would go here...</p>
      )}
    </div>
  </article>
</Layout>---
// src/pages/[slug].astro
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

// Define your static pages (modify as needed)
const staticPages = [
  { slug: 'home', title: 'Home', description: 'Welcome to my site' },
  { slug: 'about', title: 'About', description: 'About me' },
  { slug: 'contact', title: 'Contact', description: 'Get in touch' }
];

export async function getStaticPaths() {
  // Combine static pages with any dynamic content you might want
  return staticPages.map((page) => ({
    params: { slug: page.slug },
    props: { page }
  }));
}

const { page } = Astro.props;
---

<Layout 
  title={`${page.title} - The Mwaniki's Report`}
  description={page.description}
>
  <article class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4 leading-tight">
      {page.title}
    </h1>
    
    <div class="prose prose-lg max-w-none">
      <!-- Your page content here -->
      {page.slug === 'home' && (
        /* Homepage specific content */
        <p>Welcome to my homepage!</p>
      )}
      
      {page.slug === 'about' && (
        /* About page content */
        <p>About me content goes here...</p>
      )}
      
      {page.slug === 'contact' && (
        /* Contact page content */
        <p>Contact form would go here...</p>
      )}
    </div>
  </article>
</Layout>