<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gossip Feed</title>
    <style>
        /* --- LAYOUT FIXES --- */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            overflow-x: hidden; /* Prevents horizontal scroll */
        }

        /* Matches standard header/footer width */
        .main-container {
            max-width: 1200px; 
            margin: 0 auto;
            padding: 20px;
            box-sizing: border-box;
        }

        /* Grid for Posts */
        .news-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
            width: 100%;
        }

        /* Post Card Styling */
        .news-card {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            transition: transform 0.2s;
            display: flex;
            flex-direction: column;
        }

        .news-card:hover {
            transform: translateY(-5px);
        }

        .news-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .card-content {
            padding: 15px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .category-tag {
            background-color: #ff0050;
            color: white;
            padding: 4px 8px;
            text-transform: uppercase;
            font-size: 0.75rem;
            font-weight: bold;
            border-radius: 4px;
            align-self: flex-start;
            margin-bottom: 10px;
        }

        h3 {
            margin: 0 0 10px 0;
            font-size: 1.1rem;
            line-height: 1.4;
            color: #333;
        }

        p {
            font-size: 0.9rem;
            color: #666;
            flex-grow: 1;
        }

        /* --- LOAD MORE BUTTON --- */
        .load-more-container {
            text-align: center;
            margin-top: 40px;
            margin-bottom: 20px;
        }

        .load-more-btn {
            background-color: #000;
            color: #fff;
            border: none;
            padding: 12px 30px;
            font-size: 1rem;
            font-weight: bold;
            border-radius: 25px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .load-more-btn:hover {
            background-color: #333;
        }

        /* Hiding extra items initially */
        .hidden-item {
            display: none;
        }
    </style>
</head>
<body>

    <div class="main-container">
        
        <div class="news-grid" id="newsGrid">
            <article class="news-card">
                <img src="https://images.unsplash.com/photo-1499364615650-ec387c1470c5" alt="News">
                <div class="card-content">
                    <span class="category-tag">Gossip</span>
                    <h3>Savage: Vera Sidika Claps Back at Haters</h3>
                    <p>You won't believe what she said on IG live today...</p>
                </div>
            </article>
            <article class="news-card hidden-item">
                <img src="https://images.unsplash.com/photo-1504711434969-e33886168f5c" alt="News">
                <div class="card-content">
                    <span class="category-tag">Politics</span>
                    <h3>Ruto's New move shocks Gen Z</h3>
                    <p>The streets are talking and it's not looking good...</p>
                </div>
            </article>
            
            <article class="news-card hidden-item">
                <img src="https://images.unsplash.com/photo-1515378791036-0648a3ef77b2" alt="News">
                <div class="card-content">
                    <span class="category-tag">Tech</span>
                    <h3>Safaricom bundles heist? The truth exposed.</h3>
                    <p>Are we losing data or is it just 'Kanairo' vibes?</p>
                </div>
            </article>

        </div>

        <div class="load-more-container">
            <button class="load-more-btn" id="loadMoreBtn" onclick="loadMorePosts()">
                Read More Stories
            </button>
        </div>

    </div>

    <script>
        function loadMorePosts() {
            // Select all hidden items
            const hiddenItems = document.querySelectorAll('.hidden-item');
            const btn = document.getElementById('loadMoreBtn');
            
            // If no items left, hide button
            if (hiddenItems.length === 0) {
                btn.style.display = 'none';
                return;
            }

            // Reveal the next 3 items (or however many you want)
            let itemsToReveal = 3;
            for (let i = 0; i < itemsToReveal && i < hiddenItems.length; i++) {
                hiddenItems[i].classList.remove('hidden-item');
            }

            // Check again if we need to hide the button
            if (document.querySelectorAll('.hidden-item').length === 0) {
                btn.innerText = "No More Stories";
                btn.disabled = true;
                btn.style.backgroundColor = "#ccc";
                setTimeout(() => { btn.style.display = 'none'; }, 2000);
            }
        }
    </script>

</body>
</html>