---
import Layout from '../../layouts/Layout.astro';
import Article from '../../components/Article.astro';
import { getCollection } from 'astro:content';

// ... all your server-side logic (same as before) ...
---

<Layout title={title} description={description} image={imageUrl} canonical={postUrl}>
  <!-- ... all your meta tags, scripts, and main markup ... -->

  <div class="page-container">
    <!-- Breadcrumb ... -->

    <!-- Ads, nav, main content ... (no sidebar anymore) -->

    <main id="maincontent" role="main" tabindex="-1" class="main-content">
      <!-- Article, ads, author, related articles ... -->
    </main>

    <!-- Sidebar removed -->
    <!-- <aside class="sidebar" aria-label="Sidebar content"></aside> -->
  </div>

  <!-- ... scripts and footer ... -->

  <style>
    :root {
      --bg-color: #ffffff;
      --text-color: #141414;
      --max-width: 800px; /* Adjust as needed for the main article content */
      /* ... rest of your variables ... */
    }

    .page-container {
      max-width: var(--max-width);
      margin-left: auto;
      margin-right: auto;
      padding: 1rem;
      display: block; /* No grid, only one column */
      min-height: 100vh;
    }

    @media (max-width: 900px) {
      .page-container {
        padding: 1rem 0.5rem;
        max-width: 98vw;
      }
      .main-content {
        margin: 0 auto;
      }
    }

    /* Remove styles related to .sidebar and grid-template-columns/areas */

    /* ...keep your previously customized styles for featured-image, cards, etc... */
  </style>
</Layout>
