---
import { getFormattedDate } from "@/utils/date";

const { date } = Astro.props;
const formattedDate = getFormattedDate(date);
---

{formattedDate && (
  <time datetime={new Date(date).toISOString()} class={Astro.props.class}>
    {formattedDate}
  </time>
)}