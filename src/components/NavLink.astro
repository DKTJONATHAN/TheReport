---
// src/components/NavLink.astro
interface Props {
  href: string;
  mobile?: boolean;
}

const { href, mobile } = Astro.props;
---

<a 
  href={href}
  class={`${mobile ? 'block px-4 py-2 hover:bg-gray-100 rounded' : 'hover:text-blue-600 transition-colors'} font-medium`}
>
  <slot />
</a>