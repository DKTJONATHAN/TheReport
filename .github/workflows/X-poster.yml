name: 🧪 Twitter LIVE Test
on: workflow_dispatch

jobs:
  tweet:
    runs-on: ubuntu-latest
    steps:
      - name: Tweet SpaceX Post
        uses: Ethiack/twitter-api-v2-action@v1.4.1  # ✅ Verified working 2026
        with:
          api-key: ${{ secrets.TWITTER_CONSUMER_KEY }}
          api-secret: ${{ secrets.TWITTER_CONSUMER_SECRET }}
          access-token: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          access-token-secret: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
          consumer-key: ${{ secrets.TWITTER_CONSUMER_KEY }}
          consumer-secret: ${{ secrets.TWITTER_CONSUMER_SECRET }}
          endpoint: statuses/update
          status: |
            🚀 SpaceX Files for 1 Million Solar Data Centres!
            
            Game-changing orbital tech!
            
            https://jonathanmwaniki.co.ke/posts/spacex-files-for-1-million-solar-satellite-data-centres 
            
            #SpaceX #TechNews #Kenya