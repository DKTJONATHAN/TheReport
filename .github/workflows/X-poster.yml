name: 🧪 Test SpaceX Tweet (Fixed)
on: workflow_dispatch

jobs:
  tweet:
    runs-on: ubuntu-latest
    steps:
      - name: Tweet SpaceX Post
        uses: appleboy/twitter-action@v0.1.2  # ✅ Working action
        env:
          TWITTER_USERNAME: ${{ secrets.TWITTER_USERNAME }}
          TWITTER_PASSWORD: ${{ secrets.TWITTER_PASSWORD }}
          TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_TOKEN_SECRET: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
        with:
          status: |
            🚀 SpaceX Files for 1 Million Solar Satellite Data Centres
            
            Orbital computing revolution!
            
            https://jonathanmwaniki.co.ke/posts/spacex-files-for-1-million-solar-satellite-data-centres
            
            #SpaceX #TechNews #Kenya
            
      - name: Alt: Direct API Tweet
        if: failure()
        run: |
          curl -X POST "https://api.twitter.com/2/tweets" \
            -H "Authorization: Bearer ${{ secrets.TWITTER_BEARER_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{
              "text": "🚀 SpaceX Files for 1 Million Solar Satellite Data Centres

https://jonathanmwaniki.co.ke/posts/spacex-files-for-1-million-solar-satellite-data-centres

#SpaceX #TechNews"
            }'