name: Twitter Test - Minimal
on: 
  workflow_dispatch:
    inputs:
      test:
        type: boolean
        default: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Workflow WORKS - secrets next"
      
      - name: Check secrets
        env:
          CK: ${{ secrets.TWITTER_CONSUMER_KEY }}
          CS: ${{ secrets.TWITTER_CONSUMER_SECRET }}
          AT: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          ATS: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
        run: |
          echo "Keys: ${CK:0:5}... ${CS:0:5}... ${AT:0:5}... ${ATS:0:5}..."