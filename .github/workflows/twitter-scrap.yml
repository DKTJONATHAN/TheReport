name: Auto Newser

on:
  schedule:
    - cron: '0 0 * * *'   # 3AM EAT
    - cron: '0 6 * * *'   # 9AM EAT
    - cron: '0 11 * * *'  # 2PM EAT
    - cron: '0 14 * * *'  # 5PM EAT
    - cron: '0 18 * * *'  # 9PM EAT
    - cron: '0 22 * * *'  # 1AM EAT
  workflow_dispatch:
    inputs:
      manual_topic:
        description: 'Force a topic'
        required: false
        default: ''

permissions:
  contents: write

env:
  DEFAULT_BRANCH: main
  POSTS_DIR: src/content/posts

jobs:
  generate-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: ${{ env.DEFAULT_BRANCH }}
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests google-genai duckduckgo-search>=6.0.0

      - name: Generate article with Gemini
        env:
          GEMINI_WRITE_KEY: ${{ secrets.GEMINI_WRITE_KEY }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
          MANUAL_TOPIC: ${{ inputs.manual_topic }}
        run: |
          python << 'EOF'
          import os, json, datetime, requests, re, random, time
          from google import genai
          from google.genai import types
          from duckduckgo_search import DDGS

          # --- 1. CONFIGURATION ---
          date_str = datetime.datetime.utcnow().date().strftime("%Y-%m-%d")
          current_hour_utc = datetime.datetime.utcnow().hour

          manual_input = os.environ.get('MANUAL_TOPIC', '').strip().lower()
          if manual_input: 
              topic = manual_input
          else:
              if current_hour_utc in [0, 14]: topic = 'sports'
              elif current_hour_utc in [6, 22]: topic = 'technology'
              else: topic = 'business'

          print(f"🎯 Topic: {topic.upper()}")

          # --- 2. THE NYAKUNDI SEARCH (Corrected Domain) ---
          ddgs = DDGS()
          found_story = None
          
          # Priority 1: Search nyakundireport.com (Official)
          # We search for the specific topic or just "latest" if topic is generic
          # timelimit="d" ensures we only get stories indexed in the last 24h
          
          domains = ['nyakundireport.com', 'cnyakundi.com'] # Added fallback just in case
          
          for domain in domains:
              if found_story: break
              
              query = f"site:{domain} {topic}"
              print(f"1️⃣  Searching {domain}: '{query}'...")
              
              try:
                  # Strict search on the domain
                  results = list(ddgs.text(keywords=query, region="ke-en", timelimit="d", max_results=5))
                  
                  if results:
                      item = results[0]
                      found_story = {
                          'title': item['title'],
                          'href': item['href'],
                          'body': item['body'],
                          'source': 'The Nyakundi Report'
                      }
                      print(f"✅ Found Story on {domain}: {item['title']}")
              except Exception as e:
                  print(f"❌ Error searching {domain}: {e}")

          # --- 3. FALLBACK (General Kenya News) ---
          if not found_story:
              print("2️⃣  Nyakundi hasn't posted on this topic today. Falling back to General News...")
              try:
                  # Search general news but look for other blogs
                  fallback_query = f"Kenya {topic} news breaking"
                  results = list(ddgs.text(keywords=fallback_query, region="ke-en", timelimit="d", max_results=10))
                  
                  if results:
                      # Try to pick a blog if possible
                      BLOGS = ['kahawatungu', 'ghafla', 'techweez', 'tuko']
                      for item in results:
                          if any(b in item['href'] for b in BLOGS):
                              found_story = item
                              found_story['source'] = 'Alternative Blog'
                              break
                      
                      # Take top result if no blog found
                      if not found_story:
                          item = results[0]
                          found_story = {
                              'title': item['title'],
                              'href': item['href'],
                              'body': item['body'],
                              'source': 'General Media'
                          }
              except: pass

          if not found_story:
              print("❌ FATAL: No news found anywhere.")
              exit(0)

          # --- 4. DATA PREP ---
          headline = found_story['title']
          link = found_story['href']
          snippet = found_story.get('body', headline)
          source_label = found_story.get('source', 'News Source')
          
          print(f"📺 Selected Headline: {headline}")

          # --- 5. IMAGE HELPER ---
          def get_real_image(query):
              access_key = os.environ.get("UNSPLASH_ACCESS_KEY")
              fallback = f"https://image.pollinations.ai/prompt/journalistic-photo-of-{query.replace(' ', '-')}-news?width=1200&height=630&nologo=true"
              if not access_key: return fallback
              url = f"https://api.unsplash.com/photos/random?query={query}&orientation=landscape&client_id={access_key}"
              try:
                  resp = requests.get(url, timeout=5)
                  if resp.status_code == 200:
                      return resp.json()['urls']['regular']
              except: pass
              return fallback

          # --- 6. GEMINI CALL ---
          hooks = [
             "Leaked: A controversial report has just surfaced.",
             "Developing: We have obtained details on a breaking situation.",
             "Exclusive: Inside the latest report shaking the timeline."
          ]
          selected_hook = random.choice(hooks)

          prompt = f'''STRICT REPORTING MODE for jonathanmwaniki.co.ke.

          SOURCE ({source_label}):
          Headline: "{headline}"
          Snippet: "{snippet}"
          Link: {link}
          
          TASK: Write a news update (600 words).
          - Use the snippet provided to build the story. 
          - Explain who the people/companies mentioned are.
          - Tone: Fast, factual, urgent.
          - Opening: {selected_hook}

          OUTPUT FORMAT (exact):
          TITLE: [Urgent Headline, max 60 chars]
          DESCRIPTION: [Summary, max 160 chars]
          CATEGORY: [{topic.capitalize()}]
          TAGS: [3-5 tags, comma separated]
          IMAGE_KEYWORD: [2-3 words for image]
          BODY:
          [Article body with ## Headers]
          '''

          client = genai.Client(api_key=os.environ.get("GEMINI_WRITE_KEY"))
          model_id = "gemini-2.0-flash"

          print(f"🤖 Generating with {model_id}...")
          try:
              response = client.models.generate_content(
                  model=model_id,
                  contents=prompt,
                  config=types.GenerateContentConfig(temperature=0.1)
              )
              full_text = response.text.strip()
          except Exception as e:
              print(f"❌ Gemini Error: {e}")
              exit(1)

          # --- 7. PARSE & SAVE ---
          parsed = { "TITLE": headline[:60], "DESCRIPTION": headline[:160], "CATEGORY": topic.capitalize(), "TAGS": f"{topic},news,breaking", "IMAGE_KEYWORD": topic, "BODY": "" }
          current_section = None

          for line in full_text.splitlines():
              clean_line = line.strip().replace("**", "")
              if clean_line.startswith("TITLE:"): parsed["TITLE"] = clean_line.replace("TITLE:", "").strip()
              elif clean_line.startswith("DESCRIPTION:"): parsed["DESCRIPTION"] = clean_line.replace("DESCRIPTION:", "").strip()
              elif clean_line.startswith("CATEGORY:"): parsed["CATEGORY"] = clean_line.replace("CATEGORY:", "").strip()
              elif clean_line.startswith("TAGS:"): parsed["TAGS"] = clean_line.replace("TAGS:", "").strip()
              elif clean_line.startswith("IMAGE_KEYWORD:"): parsed["IMAGE_KEYWORD"] = clean_line.replace("IMAGE_KEYWORD:", "").strip()
              elif clean_line.startswith("BODY:"): current_section = "BODY"
              elif current_section == "BODY": parsed["BODY"] += line + "\n"

          image_url = get_real_image(parsed['IMAGE_KEYWORD'])
          slug = re.sub(r'[^a-z0-9-]', '-', parsed["TITLE"].lower())
          slug = re.sub(r'-+', '-', slug).strip('-')
          tag_list = [t.strip() for t in parsed["TAGS"].split(',')]

          final_file = f"""---
          title: "{parsed['TITLE'].replace('"', "'")}"
          description: "{parsed['DESCRIPTION'].replace('"', "'")}"
          date: "{date_str}"
          author: "Jonathan Mwaniki"
          image: "{image_url}"
          imageCaption: "Image for {parsed['IMAGE_KEYWORD']}"
          imageAlt: "{parsed['IMAGE_KEYWORD']}"
          category: "{parsed['CATEGORY']}"
          tags: {json.dumps(tag_list)}
          featured: true
          draft: false
          slug: "{slug}"
          ---

          # {parsed['TITLE']}

          {parsed['BODY']}

          <div class="article-meta">
            <p><strong>Published:</strong> {date_str}</p>
            <p><strong>Source:</strong> {source_label}</p>
          </div>
          """

          import textwrap
          final_file = textwrap.dedent(final_file).strip()
          out_dir = os.path.join(os.getcwd(), os.environ.get("POSTS_DIR", "src/content/posts"))
          os.makedirs(out_dir, exist_ok=True)
          with open(os.path.join(out_dir, f"{slug}.md"), "w", encoding="utf-8") as f:
              f.write(final_file)
          print(f"✅ Published: {slug}.md")
          EOF

      - name: Commit and push article
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "content: publish ${{ github.run_id }}"
          branch: ${{ env.DEFAULT_BRANCH }}
          file_pattern: ${{ env.POSTS_DIR }}/*.md
          commit_user_name: "Auto Newser"
          commit_user_email: "actions@github.com"
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}